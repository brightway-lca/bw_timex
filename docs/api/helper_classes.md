---
icon: lucide/puzzle
---

# Helper Classes

Supporting data structures used across `bw_timex`: `SetList` groups sets of activities that map to the same process across temporal databases, `TimeMappingDict` maps `(flow, timestamp)` tuples to unique integer IDs, and `InterDatabaseMapping` maps the same activity across different databases.

::: bw_timex.helper_classes
