# Edge Extractor

Extends `bw_temporalis`'s graph traversal to build a timeline of temporal edges, in addition to the timelines of flows and nodes. The resulting edge timeline is later used to match the timestamps of edges to those of time-specific background databases, and to relink them accordingly using Brightway datapackages.

::: bw_timex.edge_extractor
