# Matrix Modifier

Adds and re-links time-explicit processes in the LCA matrices. Based on a process timeline (from `TimelineBuilder.build_timeline()`), it creates datapackages that add or change entries in the technosphere and biosphere matrices, including "temporal markets" that distribute amounts across the time-specific background databases linked to a process.

::: bw_timex.matrix_modifier
