# in your terminal, run conda: conda env create -f timex-environment.yaml

name: timex_lca
channels:
  - romainsacchi
  - defaults
  - conda-forge
  - cmutel
dependencies:
  - python=3.10
  - premise
  - jupyterlab
  - matplotlib
  - unfold  
  - bw_temporalis
  - seaborn